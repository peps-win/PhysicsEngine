In progress:
Blocking for doing collision math
    -Only calculate for objects inside a specific radius around an object
Add unit testing where applicable
    -Finished for collision.rs, motion.rs, and control.rs
Fix collision problems when a ball is held
    -Every ball phases through the held ball

To Do:
Add a config menu to configure specific aspects
Add Obstacles- Allow for funnels, dividers, walls, etc
Add static obstacle drawing- When a button is held the user can draw an obstacle
Add portals that can teleport balls in obstacles
Add other shapes ( concave polygons, convex polygons, ovals, etc)